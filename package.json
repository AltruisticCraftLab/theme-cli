{
  "name": "theme-cli",
  "version": "0.1.0",
  "bin": {
    "theme": "dist/theme-cli.js"
  },
  "scripts": {
    "build": "bun build scripts/theme-cli.ts --outdir dist --target bun"
  },
  "module": "index.ts",
  "type": "module",
  "private": true,
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "fflate": "^0.8.2"
  }
}
